{
  "hosts": [
    {
      "host": "http://localhost:8887",
      "ruleSets": [
        "main"
      ]
    }
  ],
  "ruleSets": {
    "main": {
      "rules": [
        {
          "when": "/ping",
          "then": {
            "static": "Pong2!"
          }
        },
        {
          "when": "equals::/pingo",
          "then": {
            "static": "Pongo2!"
          }
        },
        {
          "when": "/",
          "then": {
            "proxy": "https://github.com/asos-craigmorten/opine-http-proxy"
          }
        }
      ]
    }
  }
}
